# Rules

- **Do not use the `rm` command to permanently delete files without explicit authorization from the user.** If a file needs to be deleted, move it to the trash bin (e.g. `~/.local/share/Trash/files/` or using the `trash-put` command if available) or explicitly ask for permission before permanently removing it.
